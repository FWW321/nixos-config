# Age key file
*.agekey

# Backup files
*.backup

# Result symlinks
result
result-*
vmtest-efi-vars.fd  # vmWithDisko 运行时 QEMU EFI 变量(CWD 生成)

# zcode 插件工作区状态(mimosa 账本/hook 状态、video2code 与 video-agent-kit
# 的 plugin_root 指针;按 workspace 落盘是插件设计,删目录会丢 mimosa 账本)
.mimosa/
.v2c/
.video_agent/
